apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: debian-api-hpa
  namespace: debian
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: debian-api-service
  minReplicas: 2
  maxReplicas: 5
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 70
