apiVersion: v1
kind: Service
metadata:
  name: debian-api-service
  namespace: debian
spec:
  type: LoadBalancer
  selector:
    app: debian-api-service
  ports:
    - name: http
      port: 80
      targetPort: 8080
